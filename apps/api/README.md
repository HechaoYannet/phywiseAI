# Phywise API

FastAPI service for OCR orchestration, structured tutoring flows, whiteboard
document generation, simulation scene rebuilding, and teacher replay APIs.

The current implementation is a protocol-first skeleton. It exposes the main
route shape and returns demo payloads that match the shared product plan.

